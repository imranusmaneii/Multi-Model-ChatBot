The image presents a detailed illustration of the Scaled Dot-Product Attention and Multi-Head Attention mechanisms, which are key components of the Transformer architecture introduced in the paper "Attention Is All You Need" by Vaswani et al. (2017).

**Figure Description:**

The figure consists of two main parts:

1. **Scaled Dot-Product Attention (Left):**
   - This mechanism takes three inputs: Query (Q), Key (K), and Value (V).
   - The process involves:
     - Computing the dot product of Q and K.
     - Scaling the result by $\frac{1}{\sqrt{d_k}}$, where $d_k$ is the dimension of the key.
     - Applying a softmax function to obtain attention weights.
     - Multiplying these weights with V to produce the output.

2. **Multi-Head Attention (Right):**
   - This mechanism consists of multiple attention layers running in parallel.
   - Each attention layer applies a linear transformation to the input (Q, K, V) and then applies Scaled Dot-Product Attention.
   - The outputs from each attention layer are concatenated and linearly transformed.

**Key Insights:**

- **Scaled Dot-Product Attention:** This attention mechanism allows the model to attend to all positions in the input sequence simultaneously and weigh their importance. The scaling factor $\frac{1}{\sqrt{d_k}}$ helps to reduce the impact of large dot product values.
- **Multi-Head Attention:** By applying multiple attention mechanisms in parallel, the model can jointly attend to information from different representation subspaces at different positions. This is beneficial for capturing complex relationships between input elements.

**Technical Details:**

- The attention function is computed as: $Attention(Q, K, V) = softmax(\frac{QK^T}{\sqrt{d_k}})V$
- The use of $h$ attention heads allows the model to capture a wider range of contextual relationships.

**Model Names and Axes:**

- The figure illustrates two specific attention mechanisms: Scaled Dot-Product Attention and Multi-Head Attention.
- The axes or inputs to these mechanisms are Query (Q), Key (K), and Value (V), which are critical in determining how the model attends to different parts of the input sequence.

**Data Points and Numerical Values:**

- The dimension of the key ($d_k$) is a critical hyperparameter that influences the scaling factor in Scaled Dot-Product Attention.
- The number of attention heads ($h$) in Multi-Head Attention allows for parallel attention mechanisms, enhancing the model's ability to capture diverse contextual information.

In summary, the figure provides a clear visual representation of the Scaled Dot-Product Attention and Multi-Head Attention mechanisms, highlighting their architectures, technical details, and the flow of data through these components. These mechanisms are foundational to the Transformer architecture, enabling efficient and effective processing of sequential data by leveraging self-attention.